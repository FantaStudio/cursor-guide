---
description: "Основные принципы FSD архитектуры - слои и их взаимодействие"
globs: ["**/*.{ts,tsx}"]
alwaysApply: true
---

# Feature-Sliced Design (FSD) Architecture

## 🏗️ Слои архитектуры (от верхнего к нижнему):

1. **app/** - инициализация приложения, провайдеры, роутинг
2. **pages/** - страницы приложения  
3. **widgets/** - сложные UI блоки
4. **features/** - бизнес-функции
5. **entities/** - бизнес-сущности и их API
6. **shared/** - переиспользуемый код

## ⚡ КРИТИЧНО: Правило импортов

**Слои могут импортировать ТОЛЬКО из нижележащих слоев!**

```typescript
// ✅ Правильно
import { Button } from "shared/ui";          // из shared
import { userApi } from "entities/user";     // из entities  
import { LoginForm } from "features/auth";   // из features

// ❌ НЕДОПУСТИМО
import { SomeWidget } from "widgets/some";   // из features ❌
import { HomePage } from "pages/home";       // из entities ❌
```

## 📁 Назначение слоев

### `app/` - Application Layer
- Настройка провайдеров (Router, Theme, Store)
- Глобальные стили и конфигурация
- Инициализация приложения

### `pages/` - Pages Layer  
- **Только композиция** widgets и features
- Роутинг на уровне страниц
- **Минимум логики**

### `widgets/` - Widgets Layer
- Самостоятельные блоки UI (Header, Sidebar)
- Композиция features и entities
- Собственное состояние виджета

### `features/` - Features Layer
- Конкретная пользовательская функциональность
- Взаимодействие с entities через их API
- Бизнес-логика фичи

### `entities/` - Entities Layer
- Модели данных и типы
- API для работы с сущностями
- Никакой UI логики

### `shared/` - Shared Layer
- UI Kit компоненты
- Утилиты и хелперы
- Базовые API клиенты
- Конфигурации

## 🎯 Структура слайса

```
slice-name/
├── index.ts         # Публичный API слайса
├── ui/              # UI компоненты (features, widgets, pages)
├── api/             # API методы (только entities, shared)
├── model/           # Типы, состояние, логика  
└── lib/             # Утилиты слайса
```

## 📦 Публичный API

**Каждый слайс экспортирует только необходимое через index.ts:**

```typescript
// features/auth/index.ts
export { LoginForm } from './ui/LoginForm';
export { useAuth } from './model/hooks';
export type { AuthUser } from './model/types';

// НЕ экспортируй внутренние детали:
// export { LoginFormField } from './ui/LoginForm/Field'; // ❌
```

## 🚫 Архитектурные запреты

1. **Нарушение зависимостей** - импорт из вышележащих слоев ❌
2. **Логика в pages** - только композиция ✅
3. **UI в entities** - только данные и API ✅
4. **Прямые API вызовы** в features - через entities ✅
5. **Циклические зависимости** между слайсами ❌

## ✅ При создании функциональности:

1. **Определи слой** - где логически должна быть функция?
2. **Проверь зависимости** - не нарушаешь ли правило импортов?
3. **Создай публичный API** - экспортируй через index.ts
4. **Следуй Single Responsibility** - одна ответственность на слайс

**Архитектура FSD обеспечивает предсказуемость, тестируемость и масштабируемость!**